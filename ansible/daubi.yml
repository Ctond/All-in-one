---

- name: change datetime
  hosts: all
  become: yes

  tasks:
  - name: change datetime
    timezone:
      name: Asia/Omsk
  
- name: ssh users and hist keys
  hosts: all
  become: yes

  tasks:
  - name: adding users
    user:
      name: user
      password: "123"

  - name: kluch
    authorized_key:
      user: test
      key: "{{ lookup('file'), /home/test/.ssh/id_rsa.pub }}"